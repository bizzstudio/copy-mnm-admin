import React from "react";
import { Navigate } from "react-router-dom";

/**
 * הפניה. המסך עצמו הוא `/integrations/finance` — אותו מסך שמשרת גם את שליחויות
 * ואת פלטפורמות המכירה, ונבדל מהם רק בקבוצה שבכתובת.
 *
 * ── למה הנתיב נשאר רשום ────────────────────────────────────────────────────
 * `/accounting/settings` היה הכתובת של המסך הזה, והוא מופיע בסימניות ובקישורים
 * פנימיים. מחיקתו הייתה מחליפה מסך עובד ב-404 בשביל שורה אחת שנחסכת.
 *
 * ── ולמה `replace` ─────────────────────────────────────────────────────────
 * בלעדיו הכתובת הישנה נשארת בהיסטוריה, ולחיצה על "חזור" מהמסך החדש מחזירה
 * להפניה שמיד קופצת קדימה שוב — לולאה שנראית למשתמש כמו כפתור חזרה שבור.
 */
const AccountingSettings = () => <Navigate to="/integrations/finance" replace />;

export default AccountingSettings;
