// src/setupTest.js
/**
 * Referenced by `vite.config.js` (`test.setupFiles`) since the project was set
 * up, but never actually committed — so `npm test` failed to collect ANY suite
 * with "Cannot find module …/src/setupTest.js", whatever the test itself said.
 *
 * Deliberately empty: nothing global is needed yet, and the point of the file is
 * that the declared configuration resolves.
 */
export {};
